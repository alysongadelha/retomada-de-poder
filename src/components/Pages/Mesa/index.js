import React from 'react';

const Mesa = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '90vh'
      }}
    >
      <h1>A Mesa</h1>
    </div>
  );
};

export default Mesa;